import type { Metadata } from "next";nimport { Geist, Geist_Mono } from "next/font/google";nimport "./globals.css";nnconst geistSans = Geist({n  variable: "--font-geist-sans",n  subsets: ["latin"],n});nnconst geistMono = Geist_Mono({n  variable: "--font-geist-mono",n  subsets: ["latin"],n});nnexport const metadata: Metadata = {n  title: "Create Next App",n  description: "Generated by create next app",n};nnexport default function RootLayout({n  children,n}: Readonly<{n  children: React.ReactNode;n}>) {n  return (n    <html lang="en">n      <bodyn        className={`${geistSans.variable} ${geistMono.variable} antialiased`}n      >n        {children}n      </body>n    </html>n  );n}